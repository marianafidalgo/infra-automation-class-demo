### Deploy Kubernetes Manifests to Local Cluster

To apply the Kubernetes manifests to your local cluster, run the following commands:

```bash
kubectl apply -f deployment.yaml
kubectl apply -f service.yaml
```